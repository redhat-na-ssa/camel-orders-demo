apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: mysql
    files:
      - a_import.sql
      - b_data.sql
generatorOptions:
  disableNameSuffixHash: true # use a static name
resources:
- kubernetes.yml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: openldap
    files:
      - ldap-example-users-2.ldif
generatorOptions:
  disableNameSuffixHash: true # use a static name
resources:
- kubernetes.yml

apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: camel-aggregator
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: camel-aggregator
  wildcardPolicy: None

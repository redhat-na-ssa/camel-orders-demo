apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: camel-processor
    behavior: replace
    files:
      - application.properties

bases:
- ../../../base/camel-processor

patchesJson6902:
- target:
    group: apps
    version: v1
    kind: Deployment
    name: camel-processor
  path: deployment_patch.yml
